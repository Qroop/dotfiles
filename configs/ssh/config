# Default (personal) GitHub identity.
# Work repos under ~/Forefront override the key via core.sshCommand
# in configs/git/gitconfig-work, so no separate Host alias is needed here.
Host github.com
	User git
	IdentityFile ~/.ssh/github_personal
	IdentitiesOnly yes
